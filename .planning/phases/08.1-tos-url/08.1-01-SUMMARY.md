---
phase: 08.1-tos-url
plan: 01
subsystem: plugin
tags: [tos, media-upload, audit, payload, ui]
requires:
  - phase: 08-zlhub-requires2
    provides: requires2 任务与审核链路基线
provides:
  - 本地路径/Base64 素材自动上传 TOS 并转换公网 URL
  - 审核链路与生成 payload 统一接入 URL 预处理
  - UI 新增并持久化 TOS 配置项
affects: [phase-08.1, video_plugin_zlhub_seedance_V2]
tech-stack:
  added: []
  patterns: [normalize-or-upload-media-url, tos-preprocess-layer]
key-files:
  created: []
  modified: [video_plugin_zlhub_seedance_V2/main.py, video_plugin_zlhub_seedance_V2/ui/index.html]
key-decisions:
  - "仅在 data:/本地路径输入时强制校验 TOS 配置，公网 URL/asset:// 透传"
  - "审核接口继续保持仅接收 http/https，预处理层负责上传转换"
patterns-established:
  - "媒体输入统一走 _normalize_or_upload_media_url"
requirements-completed: []
duration: 25min
completed: 2026-04-23
---

# Phase 08.1 Plan 01: TOS上传与素材URL预处理接入 Summary

**已完成 V2 插件的素材 URL 预处理层，打通“本地/Base64 -> TOS 公网 URL”并接入审核与生成两条链路。**

## Accomplishments

- 在 `main.py` 新增 `_ensure_tos_config`、`_upload_file_to_tos`、`_upload_data_url_to_tos`、`_normalize_or_upload_media_url`。
- 审核链路在调用 `_call_material_audit_api` 前，先对 `reference_images` 执行统一 URL 预处理。
- `_build_content_items` 改为接收 `params`，并对 image/video/audio 全量走统一预处理函数。
- UI 新增并持久化 `tos_ak/tos_sk/tos_endpoint/tos_region/tos_bucket` 五个字段。

## Verification

- `python -m py_compile video_plugin_zlhub_seedance_V2/main.py` 通过。
- `python -m pytest video_plugin_zlhub_seedance_V2/tests/test_tos_upload_pipeline.py video_plugin_zlhub_seedance_V2/tests/test_polling_limits.py video_plugin_zlhub_seedance_V2/tests/test_resolution_support.py -q` 通过（7 passed）。
- 代码检索确认 TOS 字段与 `_normalize_or_upload_media_url` 在后端/UI 均已接入。

## Notes

- 保留 `_normalize_media_url` 作为兼容包装，内部转调 `_normalize_or_upload_media_url`。
- 当前实现会在缺失 `tos` SDK 时给出 `PLUGIN_ERROR:::` 提示，避免静默失败。
