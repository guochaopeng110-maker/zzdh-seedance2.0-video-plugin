---
phase: 08.3
plan: 01
type: plan
name: 数载 Seedance 插件落地（无 content 下载端点）
objective: 基于旧版 zlhub 插件骨架交付数载插件，禁用审核链路并严格不使用 /v1/videos/{task_id}/content。
wave: 1
depends_on: [8]
files_modified: [video_plugin_shuzai_seedance/main.py, video_plugin_shuzai_seedance/ui/index.html]
autonomous: true
requirements: []
must_haves:
  - "创建/查询接口落地为 /v1/video/generations 体系"
  - "下载仅使用任务查询返回 url"
  - "代码中不得出现 /v1/videos/{task_id}/content"
  - "移除审核参数、审核分支、审核 UI"
---

# Phase 08.3 Plan 01: 数载插件实现

## Objective

- **What:** 新建并交付 `video_plugin_shuzai_seedance`。
- **Why:** 对接数载中转平台并满足用户硬约束。
- **Output:** 可执行插件 + 08.3 阶段文档闭环。

## Context

@.planning/ROADMAP.md  
@.planning/phases/08.3-shuzai-no-content-endpoint/08.3-CONTEXT.md  
@.planning/phases/08.3-shuzai-no-content-endpoint/08.3-RESEARCH.md  
@docs/require3/shuzai-seedance2.0-video-api.md  
@video_plugin_zlhub_seedance/main.py  
@video_plugin_zlhub_seedance/ui/index.html

## Wave 1

<task type="code">
<name>搭建数载插件目录并迁移宿主契约</name>
<files>video_plugin_shuzai_seedance/main.py</files>
<action>
创建独立插件目录，保留 get_info/get_params/generate/handle_action 契约，保留实时日志与任务日志能力。
</action>
<verify>
`python -m py_compile video_plugin_shuzai_seedance/main.py`
</verify>
<acceptance_criteria>
- 宿主接口函数完整存在。
- main.py 可编译。
</acceptance_criteria>
<done>插件骨架可加载</done>
</task>

<task type="code">
<name>按数载文档替换 API 协议层并移除审核链路</name>
<files>video_plugin_shuzai_seedance/main.py</files>
<action>
1. 创建任务改为 POST /v1/video/generations  
2. 查询任务改为 GET /v1/video/generations/{task_id}  
3. 删除审核参数/审核调用/审核分支
</action>
<verify>
`rg -n "audit|AuditAESCipher|/v1/video/generations" video_plugin_shuzai_seedance/main.py`
</verify>
<acceptance_criteria>
- 审核链路移除。
- 创建/查询接口路径正确。
</acceptance_criteria>
<done>主链路协议完成替换</done>
</task>

<task type="code">
<name>收敛下载策略：仅使用查询结果 url</name>
<files>video_plugin_shuzai_seedance/main.py</files>
<action>
下载函数仅接受轮询返回 url，禁止 fallback 到 /v1/videos/{task_id}/content。
</action>
<verify>
`rg -n "/v1/videos/\\{task_id\\}/content|_download_video" video_plugin_shuzai_seedance/main.py`
</verify>
<acceptance_criteria>
- content 下载端点完全不存在。
- 下载失败时给出明确 PLUGIN_ERROR。
</acceptance_criteria>
<done>下载策略符合硬约束</done>
</task>

<task type="code">
<name>更新 UI 参数面并声明约束</name>
<files>video_plugin_shuzai_seedance/ui/index.html</files>
<action>
移除审核相关控件，保留基础参数并补充“禁用 content 端点”提示。
</action>
<verify>
`rg -n "audit|/v1/videos/\\{task_id\\}/content|/v1/video/generations" video_plugin_shuzai_seedance/ui/index.html`
</verify>
<acceptance_criteria>
- UI 无审核字段。
- UI 明确显示接口约束。
</acceptance_criteria>
<done>UI 与后端协议一致</done>
</task>

## Verification

### Automated Checks

- `python -m py_compile video_plugin_shuzai_seedance/main.py`
- `python video_plugin_shuzai_seedance/main.py`
- `rg -n "/v1/videos/\\{task_id\\}/content" video_plugin_shuzai_seedance`
- `rg -n "POST /v1/video/generations|GET /v1/video/generations" docs/require3/shuzai-seedance2.0-video-api.md`

### Manual Checks

- 宿主加载 `video_plugin_shuzai_seedance` 后可打开配置页和日志页。
- 发起一次任务，观察创建->轮询->下载流程完整。
