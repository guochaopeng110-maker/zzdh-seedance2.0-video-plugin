# Phase 08.3 Discussion Log

## 2026-04-27

- 用户明确：参考 `video_plugin_zlhub_seedance`（非 V2）。
- 用户硬约束：不需要对接 `GET /v1/videos/{task_id}/content`。
- 执行决定：
  1. 新建独立目录 `video_plugin_shuzai_seedance`。
  2. 仅保留 create/query 两个任务接口。
  3. 删除图片审核接口与相关参数面。
