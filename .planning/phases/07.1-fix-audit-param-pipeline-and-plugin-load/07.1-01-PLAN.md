---
phase: 07.1-fix-audit-param-pipeline-and-plugin-load
plan: 01
type: execute
wave: 1
depends_on: []
requirements:
  - CONT-05
  - ORCH-01
  - ERR-01
files_modified:
  - video_plugin_zlhub_seedance/ui/index.html
  - video_plugin_zlhub_seedance/main.py
autonomous: true
user_setup:
  - Copy `video_plugin_zlhub_seedance` to 字字动画插件目录并重新加载插件。
  - 在插件界面填写 API Key 与 Audit User ID（如启用手动 AES Key 再填写）。
must_haves:
  truths:
    - 插件 UI 可正常加载，浏览器控制台无 `ReferenceError` / `TypeError`。
    - `audit_user_id` 在 UI 输入、保存、重新打开后可回显，并传递到后端参数。
    - `audit_aes_key` 策略明确：支持手动输入（可选）且为空时走安全回退路径，不阻塞生成流程。
    - `main.py` 通过语法编译检查，不再出现缩进错误导致插件加载失败。
  artifacts:
    - video_plugin_zlhub_seedance/ui/index.html
    - video_plugin_zlhub_seedance/main.py
  key_links:
    - from: video_plugin_zlhub_seedance/ui/index.html
      to: video_plugin_zlhub_seedance/main.py
      via: PluginSDK.saveParam -> get_params -> _sanitize_params -> _run_seedance_orchestration
      pattern: audit_user_id
---

# Phase 7.1 Plan 01: Fix audit params pipeline and plugin load stability

<objective>
Stabilize plugin loading and complete the audit parameter pipeline from UI input persistence to backend audit execution paths.
</objective>

## Objective

- **What:** 修复 `video_plugin_zlhub_seedance` 在 UI 与后端之间的审核参数链路，并消除插件加载阶段的致命错误。
- **Why:** 当前存在缺失输入框、变量名拼写错误、未声明 DOM 变量与疑似缩进异常，导致插件配置不可用或加载失败。
- **Output:** 可加载插件版本（UI/参数链路/语法校验通过），并附带最小可复现验证步骤。

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@video_plugin_zlhub_seedance/ui/index.html
@video_plugin_zlhub_seedance/main.py

## Tasks

<task type="code">
  <name>Repair UI controls and DOM bindings for audit fields</name>
  <files>video_plugin_zlhub_seedance/ui/index.html</files>
  <action>
    Add missing `Audit User ID` input and wire it with `PluginSDK.saveParam('audit_user_id', ...)`.
    Introduce a clear `audit_aes_key` interaction policy in UI:
    - provide an optional password input (manual mode) and correct save/load bindings, or
    - keep hidden by default but ensure value source is explicit in UI hint.
    Fix current runtime breakers:
    - define missing DOM refs (`videoStyleSelect`, audit inputs),
    - fix typo `auditAesKeyput` -> `auditAesKeyInput`,
    - ensure `onReady` only references existing elements.
  </action>
  <verify>
    Reload plugin UI and confirm no console errors.
    Change/reopen config and verify `api_key`, `video_style`, `audit_user_id`, `audit_aes_key` persistence behaves as designed.
  </verify>
  <done>
    UI renders all required controls, event listeners fire, and saved params can be read back without JS errors.
  </done>
</task>

<task type="code">
  <name>Harden backend audit param sanitization and fallback behavior</name>
  <files>video_plugin_zlhub_seedance/main.py</files>
  <action>
    Verify `_build_default_params`, `_sanitize_params`, and `_run_seedance_orchestration` consume audit params consistently.
    Ensure audit call path in human-style mode handles:
    - valid `audit_user_id`,
    - optional `audit_aes_key` (manual value or fallback strategy),
    - clear `PLUGIN_ERROR:::` diagnostics on missing/invalid audit prerequisites.
    Align log fields so task metadata can prove whether audit branch was triggered.
  </action>
  <verify>
    Run targeted dry-run or mocked path to assert:
    - non-human style skips audit,
    - human style + refs triggers audit branch,
    - returned asset URLs flow into payload construction.
  </verify>
  <done>
    Audit branch behavior is deterministic, observable in logs, and no regression on non-audit generation path.
  </done>
</task>

<task type="code">
  <name>Eliminate plugin load blockers and validate packaging copy</name>
  <files>video_plugin_zlhub_seedance/main.py, video_plugin_zlhub_seedance/ui/index.html</files>
  <action>
    Run Python syntax check (py_compile) to catch indentation issues before distribution.
    Perform plugin-directory copy validation and startup smoke check in host app.
    If load still fails, collect exact traceback and fix immediately in source copy path parity.
  </action>
  <verify>
    python -m py_compile video_plugin_zlhub_seedance/main.py passes.
    Host app can load plugin after copy, open settings page, and invoke generate once without startup exceptions.
  </verify>
  <done>
    No startup syntax/runtime crash remains; copied plugin behavior matches repository source.
  </done>
</task>

## Verification

1. Static checks:
   - node .codex/get-shit-done/bin/gsd-tools.cjs verify plan-structure .planning/phases/07.1-fix-audit-param-pipeline-and-plugin-load/07.1-01-PLAN.md
   - python -m py_compile video_plugin_zlhub_seedance/main.py
2. Runtime checks (host plugin):
   - 插件面板加载成功，字段可编辑且可保存回显。
   - 选择“仿真人风格”并提供参考图后，日志可见审核链路触发。
   - 选择“其他风格”时不触发审核链路，生成主链路保持可用。

## Success Criteria

- [ ] UI 中审核参数交互可用且无未定义变量错误。
- [ ] 后端 audit 参数链路贯通，日志可观测。
- [ ] `main.py` 在仓库与拷贝目录均无缩进/语法错误。
- [ ] 插件在字字动画中成功加载并完成至少一次生成流程。
