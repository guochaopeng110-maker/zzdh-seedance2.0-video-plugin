---
phase: 07.1-fix-audit-param-pipeline-and-plugin-load
plan: 01
subsystem: "[primary category]"
tags:
  - plugin
  - audit
  - bugfix
provides:
  - 修复审核参数在 UI 与后端之间的传递链路
  - 修复插件设置页加载时的前端引用错误
affects:
  - video_plugin_zlhub_seedance/ui/index.html
  - video_plugin_zlhub_seedance/main.py
tech-stack:
  added:
    - cryptography (已有环境依赖，代码中补齐导入与可用性检查)
  patterns: []
key-files:
  created: []
  modified:
    - video_plugin_zlhub_seedance/ui/index.html
    - video_plugin_zlhub_seedance/main.py
key-decisions:
  - 审核 AES Key 改为插件内固定值，移除 UI/环境变量读取
  - 新增 `audit_test_only` 开关，审核测试时跳过视频生成接口避免计费
  - Asset 协议识别改为大小写不敏感，兼容 Asset:// 与 asset://
patterns-established: []
duration: "55min"
completed: 2026-04-09
---

# Phase 7.1: Fix audit params pipeline and plugin load stability Summary

**已修复审核参数链路与插件加载关键故障，仿真人审核分支可执行且普通分支不受影响。**

## Performance

- **Duration:** 55min
- **Tasks:** 3/3 completed
- **Files modified:** 2

## Accomplishments

1. 修复 UI 层缺失控件与未声明变量问题：新增 `Audit User ID` 与“仅测试审核接口”开关，移除手动 AES Key 输入并改为固定密钥提示，补全 `video_style` / `audit_test_only` 参数的保存与回显绑定。
2. 补齐后端审核核心函数 `_call_material_audit_api`：实现 AES-256-ECB-PKCS7 加解密请求/响应、错误处理、`Asset://` 资源提取及数量校验。
3. 增强兼容性与稳定性：补齐 `cryptography` 导入与缺失提示、默认参数包含审核字段、`Asset://` 协议识别改为大小写不敏感，避免审核结果被误当成本地路径。

## Task Commits

1. **Task 1: Repair UI controls and DOM bindings for audit fields** - 未提交（工作区执行）
2. **Task 2: Harden backend audit param sanitization and fallback behavior** - 未提交（工作区执行）
3. **Task 3: Eliminate plugin load blockers and validate packaging copy** - 未提交（工作区执行）

## Files Created/Modified

1. `video_plugin_zlhub_seedance/ui/index.html` - 新增审核参数输入控件、补全 save/load 事件链路、修正 model 选项 value。
2. `video_plugin_zlhub_seedance/main.py` - 新增审核 API 调用函数与 AES 依赖导入，完善审核参数默认值与协议兼容逻辑。

## Decisions & Deviations

与上一版相比，本次将 `audit_aes_key` 固定在插件内部，并增加 `audit_test_only` 测试模式用于验证审核接口而不触发付费生成；其余实现遵循既定计划。

## Next Phase Readiness

可直接在宿主中执行回归验证：复制插件目录后加载 UI，填写参数并分别验证“仿真人风格”和“其他风格”两条链路。
