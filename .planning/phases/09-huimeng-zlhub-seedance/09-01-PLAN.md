---
phase: 09
plan: 01
type: plan
name: Huimeng Seedance 视频插件落地
objective: 交付 video_plugin_huimeng_seedance，完成 Huimeng 视频任务主链路与模型差异化参数映射，满足宿主可加载与可观测性要求。
wave: 1
depends_on: [8]
files_modified: [video_plugin_huimeng_seedance/main.py, video_plugin_huimeng_seedance/ui/index.html]
autonomous: true
requirements: []
must_haves:
  - "插件目录固定为 video_plugin_huimeng_seedance，宿主契约完整"
  - "UI 提供 5 个模型 ID 选择"
  - "happyhorse-1.0 不传 human_review/reference_videos/reference_audios"
  - "各模型 ratio/resolution 可选值严格遵循文档"
  - "主链路为 POST /api/v1/tasks -> GET /api/v1/tasks/{task_id} -> result.video_url 下载"
  - "错误统一 PLUGIN_ERROR:::，包含进度回调与日志"
---

# Phase 09 Plan 01: Huimeng 插件实现

## Objective

- **What:** 实现 `video_plugin_huimeng_seedance`。
- **Why:** 新增 Huimeng 视频中转平台能力并复用成熟宿主插件机制。
- **Output:** 可被宿主加载的 Huimeng 视频插件 + UI + 验证闭环。

## Context

@.planning/ROADMAP.md
@.planning/phases/09-huimeng-zlhub-seedance/09-CONTEXT.md
@.planning/phases/09-huimeng-zlhub-seedance/09-RESEARCH.md
@docs/require4/huimeng-image-video-api.md
@docs/require4/视频模型的params参数说明.md
@video_plugin_zlhub_seedance/main.py
@video_plugin_zlhub_seedance/ui/index.html

## Wave 1

<task type="code">
<name>创建 Huimeng 插件目录并迁移宿主契约骨架</name>
<files>video_plugin_huimeng_seedance/main.py</files>
<read_first>
- video_plugin_zlhub_seedance/main.py
- .planning/phases/09-huimeng-zlhub-seedance/09-CONTEXT.md
</read_first>
<action>
创建 `video_plugin_huimeng_seedance/main.py`，保留宿主契约函数 `get_info` / `get_params` / `generate`，保留 `PLUGIN_ERROR:::` 错误封装、进度回调与日志框架。
</action>
<verify>
`python -m py_compile video_plugin_huimeng_seedance/main.py`
</verify>
<acceptance_criteria>
- `video_plugin_huimeng_seedance/main.py` 存在且包含 `def get_info(`、`def get_params(`、`def generate(`。
- 文件中存在 `PLUGIN_ERROR:::` 常量。
- `python -m py_compile video_plugin_huimeng_seedance/main.py` 退出码为 0。
</acceptance_criteria>
<done>插件基础骨架可被宿主识别</done>
</task>

<task type="code">
<name>实现 Huimeng API 客户端与异步任务编排</name>
<files>video_plugin_huimeng_seedance/main.py</files>
<read_first>
- docs/require4/huimeng-image-video-api.md
- video_plugin_zlhub_seedance/main.py
</read_first>
<action>
实现 Huimeng API 调用：创建任务使用 `POST /api/v1/tasks`，查询任务使用 `GET /api/v1/tasks/{task_id}`，完成状态从 `result.video_url` 下载 mp4；鉴权头为 `Authorization: Bearer hm-...`；失败状态读取 `error_message` 并转换为 `PLUGIN_ERROR:::`。
</action>
<verify>
`rg -n "POST /api/v1/tasks|GET /api/v1/tasks/\\{task_id\\}|Authorization: Bearer|error_message|video_url" video_plugin_huimeng_seedance/main.py`
</verify>
<acceptance_criteria>
- 代码中存在创建与查询端点字符串：`/api/v1/tasks` 与 `/api/v1/tasks/{task_id}`。
- 下载逻辑使用查询结果字段 `result.video_url`。
- 失败映射中包含 `error_message`。
</acceptance_criteria>
<done>Huimeng 主链路可执行</done>
</task>

<task type="code">
<name>实现模型差异化 params 映射与本地校验</name>
<files>video_plugin_huimeng_seedance/main.py</files>
<read_first>
- docs/require4/视频模型的params参数说明.md
- .planning/phases/09-huimeng-zlhub-seedance/09-CONTEXT.md
</read_first>
<action>
实现 5 模型映射：`happyhorse-1.0`、`seedance-2.0-value`、`seedance-2.0-fast-value`、`seedance-2.0`、`seedance-2.0-fast`。按模型维度维护 `ratio/resolution` 可选值白名单；`happyhorse-1.0` payload 中禁止传 `human_review`、`reference_videos`、`reference_audios`；其余 seedance 模型按文档支持相应字段。
</action>
<verify>
`rg -n "happyhorse-1.0|seedance-2.0-value|seedance-2.0-fast-value|seedance-2.0-fast|human_review|reference_videos|reference_audios|ratio|resolution" video_plugin_huimeng_seedance/main.py`
</verify>
<acceptance_criteria>
- 文件中显式包含 5 个模型 ID 字符串。
- 存在模型级别的 `ratio` 与 `resolution` 可选值映射结构。
- `happyhorse-1.0` 构造 payload 分支中不包含 `human_review`、`reference_videos`、`reference_audios`。
</acceptance_criteria>
<done>模型参数差异正确落地</done>
</task>

<task type="code">
<name>实现参考素材公网 URL 预处理与上传回填</name>
<files>video_plugin_huimeng_seedance/main.py</files>
<read_first>
- docs/require4/huimeng-image-video-api.md
- .planning/phases/09-huimeng-zlhub-seedance/09-RESEARCH.md
</read_first>
<action>
实现参考素材预处理层：当 `reference_images` 含本地文件路径时，调用上传接口将其转换为公网 URL 后再组装 payload；上传失败立即抛出 `PLUGIN_ERROR:::`。远程 URL 输入直接透传。
</action>
<verify>
`rg -n "imageproxy\\.zhongzhuan\\.chat|upload|reference_images|PLUGIN_ERROR" video_plugin_huimeng_seedance/main.py`
</verify>
<acceptance_criteria>
- 代码中存在上传接口 URL 配置与调用。
- 本地路径与公网 URL 的分支逻辑可辨识。
- 上传失败路径返回 `PLUGIN_ERROR:::` 错误。
</acceptance_criteria>
<done>素材输入满足 Huimeng URL 约束</done>
</task>

<task type="code">
<name>实现 UI 参数面：5 模型选择 + 模型驱动字段约束提示</name>
<files>video_plugin_huimeng_seedance/ui/index.html</files>
<read_first>
- video_plugin_zlhub_seedance/ui/index.html
- docs/require4/视频模型的params参数说明.md
</read_first>
<action>
创建 `video_plugin_huimeng_seedance/ui/index.html`，提供 5 模型下拉选择；对 `ratio`、`resolution` 等字段按当前模型刷新可选值；UI 明确提示不同模型参数差异（特别是 happyhorse 不含 `human_review/reference_videos/reference_audios`）。
</action>
<verify>
`rg -n "happyhorse-1.0|seedance-2.0-value|seedance-2.0-fast-value|seedance-2.0|seedance-2.0-fast|ratio|resolution|human_review|reference_videos|reference_audios" video_plugin_huimeng_seedance/ui/index.html`
</verify>
<acceptance_criteria>
- `ui/index.html` 包含 5 个模型 ID 选项。
- `ratio`、`resolution` 的选项源可按模型区分。
- 页面含有 happyhorse 参数差异说明文本。
</acceptance_criteria>
<done>UI 与参数映射一致</done>
</task>

## Verification

### Automated Checks
- `python -m py_compile video_plugin_huimeng_seedance/main.py`
- `python video_plugin_huimeng_seedance/main.py`
- `rg -n "happyhorse-1.0|seedance-2.0-value|seedance-2.0-fast-value|seedance-2.0|seedance-2.0-fast" video_plugin_huimeng_seedance`
- `rg -n "human_review|reference_videos|reference_audios" video_plugin_huimeng_seedance/main.py`
- `rg -n "/api/v1/tasks|result.video_url|PLUGIN_ERROR:::" video_plugin_huimeng_seedance/main.py`

### Manual Checks
- 在字字动画宿主加载 `video_plugin_huimeng_seedance` 并可打开配置界面。
- 切换 5 个模型时，`ratio` 与 `resolution` 可选值按模型变化。
- 使用 `happyhorse-1.0` 触发一次任务，确认 payload 不包含 `human_review/reference_videos/reference_audios`。
- 使用任一 seedance 模型触发一次任务，确认创建/轮询/下载链路完整。
